# TIM 需求


### LRS.PERF.SPI_MASTER.TIM.001

<!-- LRS_META
id: LRS.PERF.SPI_MASTER.TIM.001
category: PERF
feature: TIM-001
priority: P0
status: active
source_ref:
- spi_master_contract.md#TIM-001
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

CPHA=0，首个有效 MOSI bit 应在首个 leading edge 前建立；相邻帧连续传输时，在前帧最后 trailing edge 装载下一帧首 bit。CPHA=1，在首个 leading edge 移出首 bit，在 trailing edge 采样。

#### Acceptance Criteria

- 对照原合同 TIM-001 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.PERF.SPI_MASTER.TIM.002

<!-- LRS_META
id: LRS.PERF.SPI_MASTER.TIM.002
category: PERF
feature: TIM-002
priority: P0
status: active
source_ref:
- spi_master_contract.md#TIM-002
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

每个正常完成 frame 必须生成恰好 `2 × FRAME_BITS` 个 SCLK 边沿，包含返回 CPOL 的最后 trailing edge；不得以最后一次 MISO 采样替代整个 frame 完成。

#### Acceptance Criteria

- 对照原合同 TIM-002 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.PERF.SPI_MASTER.TIM.003

<!-- LRS_META
id: LRS.PERF.SPI_MASTER.TIM.003
category: PERF
feature: TIM-003
priority: P0
status: active
source_ref:
- spi_master_contract.md#TIM-003
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

SCLK 半周期为 `H = CLKDIV + 1` 个 PCLK，CLKDIV 为 16 位无符号数：

#### Acceptance Criteria

- 对照原合同 TIM-003 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.PERF.SPI_MASTER.TIM.004

<!-- LRS_META
id: LRS.PERF.SPI_MASTER.TIM.004
category: PERF
feature: TIM-004
priority: P0
status: active
source_ref:
- spi_master_contract.md#TIM-004
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

每个 CS 独立配置以下时序，字段单位均为 PCLK，避免软件对不同 SCLK 重新解释计数值：

#### Acceptance Criteria

- 对照原合同 TIM-004 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.PERF.SPI_MASTER.TIM.005

<!-- LRS_META
id: LRS.PERF.SPI_MASTER.TIM.005
category: PERF
feature: TIM-005
priority: P0
status: active
source_ref:
- spi_master_contract.md#TIM-005
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

切换器件/CPOL 时：先以旧配置完成 hold 并释放全部 CS，等待旧 CS_IDLE；再更新 SCLK 空闲电平，等待新 CS_IDLE，方可断言新 CS 并执行 setup。若 CPOL 不变，允许合并等待为 `max(old_idle,new_idle)`。CS 有效时严禁因配置写产生 SCLK 跳变。

#### Acceptance Criteria

- 对照原合同 TIM-005 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.PERF.SPI_MASTER.TIM.006

<!-- LRS_META
id: LRS.PERF.SPI_MASTER.TIM.006
category: PERF
feature: TIM-006
priority: P0
status: active
source_ref:
- spi_master_contract.md#TIM-006
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

空闲时 SCLK 保持最近一次实际启用的 CPOL；写配置不立即翻转输出。复位 SCLK=0。MOSI 在活动帧按位输出；正常段间等待保留末位；事务释放后置 0。

#### Acceptance Criteria

- 对照原合同 TIM-006 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。
