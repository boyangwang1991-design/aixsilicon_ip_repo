# CS 需求


### LRS.FUNC.SPI_MASTER.CS.001

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CS.001
category: FUNC
feature: CS-001
priority: P0
status: active
source_ref:
- spi_master_contract.md#CS-001
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

任意时刻最多一个 CS 有效。KEEP_CS=1 保持当前 CS，便于组合指令、地址、Dummy、读数据等阶段。

#### Acceptance Criteria

- 对照原合同 CS-001 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.FUNC.SPI_MASTER.CS.002

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CS.002
category: FUNC
feature: CS-002
priority: P0
status: active
source_ref:
- spi_master_contract.md#CS-002
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

保持期间后续命令必须为同一 CS。CS 不匹配在执行前检出，进入故障恢复，不允许悄悄释放旧 CS 后转移到新器件。正常切换器件须先用 KEEP_CS=0 的末段或 RELEASE 显式结束旧事务。

#### Acceptance Criteria

- 对照原合同 CS-002 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.FUNC.SPI_MASTER.CS.003

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CS.003
category: FUNC
feature: CS-003
priority: P0
status: active
source_ref:
- spi_master_contract.md#CS-003
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

保持期间下一命令尚未到达时，SCLK 保持 CPOL、CS 保持有效，WAIT_CMD=1；适用等待超时。软件须确保目标器件允许该停顿。KEEP_CS 不等价于 SCLK 连续。

#### Acceptance Criteria

- 对照原合同 CS-003 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.FUNC.SPI_MASTER.CS.004

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CS.004
category: FUNC
feature: CS-004
priority: P0
status: active
source_ref:
- spi_master_contract.md#CS-004
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

无 stall 的同段连续帧在 FRAME_GAP=0 时，应保持连续 SCLK，前提是下一帧所需资源在前帧最后 trailing edge 前至少一个 PCLK 已可用。RTL 应预取或等效提前判定。段间不保证连续 SCLK；需要严格无间断的协议应组织为单个 TXRX/TX/RX segment。

#### Acceptance Criteria

- 对照原合同 CS-004 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.FUNC.SPI_MASTER.CS.005

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CS.005
category: FUNC
feature: CS-005
priority: P0
status: active
source_ref:
- spi_master_contract.md#CS-005
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

CS 自动覆盖整个 segment，不在每帧间翻转。如器件要求每个字单独片选，应提交多个单 frame、KEEP_CS=0 的 segment。

#### Acceptance Criteria

- 对照原合同 CS-005 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。
