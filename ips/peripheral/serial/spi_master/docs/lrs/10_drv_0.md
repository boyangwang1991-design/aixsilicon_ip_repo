# DRV 需求


### LRS.CONS.SPI_MASTER.DRV.001

<!-- LRS_META
id: LRS.CONS.SPI_MASTER.DRV.001
category: CONS
feature: DRV-001
priority: P0
status: active
source_ref:
- spi_master_contract.md#DRV-001
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

控制器由单个驱动实例串行管理。多线程/CPU/DMA 不得在无锁条件下交错访问 CMD shadow/PUSH 或 TXDATA/RXDATA；驱动对命令构造和对应数据序列加锁，并使用平台规定的 MMIO 顺序屏障。

#### Acceptance Criteria

- 对照原合同 DRV-001 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.CONS.SPI_MASTER.DRV.002

<!-- LRS_META
id: LRS.CONS.SPI_MASTER.DRV.002
category: CONS
feature: DRV-002
priority: P0
status: active
source_ref:
- spi_master_contract.md#DRV-002
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

轮询必须包含软件总期限；WAIT_TIMEOUT 只覆盖资源等待，不能替代总期限。SEG_DONE sticky 可合并多个完成事件，驱动根据 DONE_COUNT 差值和软件队列核对；TAG 不强制唯一，不提供完整 completion FIFO。

#### Acceptance Criteria

- 对照原合同 DRV-002 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。
