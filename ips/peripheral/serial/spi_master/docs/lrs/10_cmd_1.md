# CMD 需求


### LRS.FUNC.SPI_MASTER.CMD.011

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CMD.011
category: FUNC
feature: CMD-011
priority: P0
status: active
source_ref:
- spi_master_contract.md#CMD-011
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

SEG_DONE 表示 descriptor 完成；XFER_DONE 仅在一个实际断言过的 CS 被正常释放时产生。空闲 RELEASE 只有 SEG_DONE。命令 FIFO 空、TX FIFO 空或最后 TX entry 被弹出均不是完成事件。

#### Acceptance Criteria

- 对照原合同 CMD-011 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.FUNC.SPI_MASTER.CMD.012

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CMD.012
category: FUNC
feature: CMD-012
priority: P0
status: active
source_ref:
- spi_master_contract.md#CMD-012
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

BUSY 从命令被接受开始覆盖排队、准备、执行、hold、idle 等待、KEEP_CS 等待及中止过程。仅在无活动段、无待执行命令、无有效 CS、无剩余时序等待及中止时清 0；TX/RX FIFO 残留不单独置 BUSY。

#### Acceptance Criteria

- 对照原合同 CMD-012 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。
