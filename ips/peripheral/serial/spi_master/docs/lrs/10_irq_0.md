# IRQ 需求


### LRS.REG.SPI_MASTER.IRQ.001

<!-- LRS_META
id: LRS.REG.SPI_MASTER.IRQ.001
category: REG
feature: IRQ-001
priority: P0
status: active
source_ref:
- spi_master_contract.md#IRQ-001
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

IRQ_STATE 只有 bit[2:0] 可读写；IRQ_RAW[2:0] 来自 IRQ_STATE，bit[5:3] 来自实时条件。`irq_o = OR(IRQ_RAW & IRQ_ENABLE)`。IRQ_ENABLE 仅 bit[5:0] 有效。复位所有使能为 0。

#### Acceptance Criteria

- 对照原合同 IRQ-001 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.REG.SPI_MASTER.IRQ.002

<!-- LRS_META
id: LRS.REG.SPI_MASTER.IRQ.002
category: REG
feature: IRQ-002
priority: P0
status: active
source_ref:
- spi_master_contract.md#IRQ-002
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

清 IRQ_STATE 不能清电平源；ERROR 必须清 ERROR_STATUS，TX/RX 水位必须服务 FIFO 或屏蔽。事件发生与 W1C 同沿时置位优先。读取状态无清除副作用。

#### Acceptance Criteria

- 对照原合同 IRQ-002 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.REG.SPI_MASTER.IRQ.003

<!-- LRS_META
id: LRS.REG.SPI_MASTER.IRQ.003
category: REG
feature: IRQ-003
priority: P0
status: active
source_ref:
- spi_master_contract.md#IRQ-003
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

最后一次接收可能不足 RX_WM，驱动在 SEG_DONE/XFER_DONE 后仍须读取 RX_LEVEL 并排空尾数据。TX 水位在控制器使能但空闲时亦可能持续成立，驱动不供数时应屏蔽该源。

#### Acceptance Criteria

- 对照原合同 IRQ-003 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。
