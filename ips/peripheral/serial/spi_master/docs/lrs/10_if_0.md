# IF 需求


### LRS.INTF.SPI_MASTER.IF.001

<!-- LRS_META
id: LRS.INTF.SPI_MASTER.IF.001
category: INTF
feature: IF-001
priority: P0
status: active
source_ref:
- spi_master_contract.md#IF-001
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

V1.0 无 IO 三态控制；SCLK/MOSI/CS 为专用推挽输出，Pad 和 pinmux 属于 SoC。RX-only 时 MOSI 仍输出 dummy pattern。MISO 应由板级/Pad 保证未选中器件释放总线。

#### Acceptance Criteria

- 对照原合同 IF-001 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.INTF.SPI_MASTER.IF.002

<!-- LRS_META
id: LRS.INTF.SPI_MASTER.IF.002
category: INTF
feature: IF-002
priority: P0
status: active
source_ref:
- spi_master_contract.md#IF-002
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

全部内部状态使用 `pclk`；不得以输出 SCLK 作为内部逻辑时钟。通过 clock enable 产生串行边沿及采样事件。

#### Acceptance Criteria

- 对照原合同 IF-002 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.INTF.SPI_MASTER.IF.003

<!-- LRS_META
id: LRS.INTF.SPI_MASTER.IF.003
category: INTF
feature: IF-003
priority: P0
status: active
source_ref:
- spi_master_contract.md#IF-003
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

MISO 是受外发 SCLK 和外设输出延迟约束的返回路径。必须给出 IO 时序预算及 STA 约束；不得将其简单标成异步 false path 后宣称时序通过，也不得无补偿地加入两级同步器而造成接收错位。

#### Acceptance Criteria

- 对照原合同 IF-003 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。
