# APB 需求


### LRS.INTF.SPI_MASTER.APB.001

<!-- LRS_META
id: LRS.INTF.SPI_MASTER.APB.001
category: INTF
feature: APB-001
priority: P0
status: active
source_ref:
- spi_master_contract.md#APB-001
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

副作用仅在 `psel && penable && pready` 的上升沿提交；SETUP 阶段不得提交

#### Acceptance Criteria

- 对照原合同 APB-001 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.INTF.SPI_MASTER.APB.002

<!-- LRS_META
id: LRS.INTF.SPI_MASTER.APB.002
category: INTF
feature: APB-002
priority: P0
status: active
source_ref:
- spi_master_contract.md#APB-002
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

正常运行时首个 ACCESS 周期 `pready=1`，零 wait-state；不得等待 SPI 完成、FIFO 可用或软件恢复

#### Acceptance Criteria

- 对照原合同 APB-002 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.INTF.SPI_MASTER.APB.003

<!-- LRS_META
id: LRS.INTF.SPI_MASTER.APB.003
category: INTF
feature: APB-003
priority: P0
status: active
source_ref:
- spi_master_contract.md#APB-003
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

支持同一 PSEL 下连续传输；每次均需合法 SETUP/ACCESS，恰好一次寄存器/FIFO 操作

#### Acceptance Criteria

- 对照原合同 APB-003 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.INTF.SPI_MASTER.APB.004

<!-- LRS_META
id: LRS.INTF.SPI_MASTER.APB.004
category: INTF
feature: APB-004
priority: P0
status: active
source_ref:
- spi_master_contract.md#APB-004
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

错误仅在完成周期以 PSLVERR 指示；失败读返回 0；失败访问不得部分修改目标寄存器或 FIFO

#### Acceptance Criteria

- 对照原合同 APB-004 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.INTF.SPI_MASTER.APB.005

<!-- LRS_META
id: LRS.INTF.SPI_MASTER.APB.005
category: INTF
feature: APB-005
priority: P0
status: active
source_ref:
- spi_master_contract.md#APB-005
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

所有 CSR 地址必须 4 字节对齐。非对齐、未映射、访问权限错误返回 PSLVERR

#### Acceptance Criteria

- 对照原合同 APB-005 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.INTF.SPI_MASTER.APB.006

<!-- LRS_META
id: LRS.INTF.SPI_MASTER.APB.006
category: INTF
feature: APB-006
priority: P0
status: active
source_ref:
- spi_master_contract.md#APB-006
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

普通 RW 字段按 PSTRB byte lane 更新；写零 strobe 对合法可写地址成功且无副作用

#### Acceptance Criteria

- 对照原合同 APB-006 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.INTF.SPI_MASTER.APB.007

<!-- LRS_META
id: LRS.INTF.SPI_MASTER.APB.007
category: INTF
feature: APB-007
priority: P0
status: active
source_ref:
- spi_master_contract.md#APB-007
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

PSTRB 不用于读；APB 本身不携带访问长度。本 IP 的 RXDATA 一次成功读固定弹出一个 entry

#### Acceptance Criteria

- 对照原合同 APB-007 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.INTF.SPI_MASTER.APB.008

<!-- LRS_META
id: LRS.INTF.SPI_MASTER.APB.008
category: INTF
feature: APB-008
priority: P0
status: active
source_ref:
- spi_master_contract.md#APB-008
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

PPROT 接入但本版本不实施权限过滤。全部合法属性取值功能一致；访问控制由上游完成

#### Acceptance Criteria

- 对照原合同 APB-008 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.INTF.SPI_MASTER.APB.009

<!-- LRS_META
id: LRS.INTF.SPI_MASTER.APB.009
category: INTF
feature: APB-009
priority: P0
status: active
source_ref:
- spi_master_contract.md#APB-009
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

非零 strobe 的 TXDATA、CMD_PUSH、ACTION 写必须 `PSTRB=4'b1111`，否则报错

#### Acceptance Criteria

- 对照原合同 APB-009 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.INTF.SPI_MASTER.APB.010

<!-- LRS_META
id: LRS.INTF.SPI_MASTER.APB.010
category: INTF
feature: APB-010
priority: P0
status: active
source_ref:
- spi_master_contract.md#APB-010
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

RO 写、WO 读报错；保留字段读 0、写忽略；W1C 只清除选中字节中写 1 的位

#### Acceptance Criteria

- 对照原合同 APB-010 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。
