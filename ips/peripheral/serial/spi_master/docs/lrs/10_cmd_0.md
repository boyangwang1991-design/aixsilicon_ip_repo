# CMD 需求


### LRS.FUNC.SPI_MASTER.CMD.001

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CMD.001
category: FUNC
feature: CMD-001
priority: P0
status: active
source_ref:
- spi_master_contract.md#CMD-001
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

segment 为一个 descriptor 描述的重复操作；transaction 为同一个 CS 从断言到释放之间的一组 segment。命令 FIFO 以提交顺序执行，不重排。

#### Acceptance Criteria

- 对照原合同 CMD-001 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.FUNC.SPI_MASTER.CMD.002

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CMD.002
category: FUNC
feature: CMD-002
priority: P0
status: active
source_ref:
- spi_master_contract.md#CMD-002
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

软件写 CMD_CFG/CMD_LEN/CMD_TAG，再写 CMD_PUSH bit0=1。仅 CMD_PUSH 成功的 APB 完成沿，将三个 shadow 寄存器原子快照入队。后续修改 shadow 不影响已入队命令。不需要独立 START。

#### Acceptance Criteria

- 对照原合同 CMD-002 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.FUNC.SPI_MASTER.CMD.003

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CMD.003
category: FUNC
feature: CMD-003
priority: P0
status: active
source_ref:
- spi_master_contract.md#CMD-003
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

命令包含：`CSID, OP, FRAME_BITS, KEEP_CS, ALLOW_STALL, TAG, LEN`。OP 如下：

#### Acceptance Criteria

- 对照原合同 CMD-003 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.FUNC.SPI_MASTER.CMD.004

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CMD.004
category: FUNC
feature: CMD-004
priority: P0
status: active
source_ref:
- spi_master_contract.md#CMD-004
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

数据/DUMMY 命令 LEN 为实际数量，支持 1～65535；0 非法，不采用减一编码。数据命令 FRAME_BITS 采用 1～32 实际值。DUMMY/RELEASE 要求 FRAME_BITS=0。RELEASE 要求 KEEP_CS=0、ALLOW_STALL=0。

#### Acceptance Criteria

- 对照原合同 CMD-004 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.FUNC.SPI_MASTER.CMD.005

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CMD.005
category: FUNC
feature: CMD-005
priority: P0
status: active
source_ref:
- spi_master_contract.md#CMD-005
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

CSID 必须小于 NUM_CS。控制器 ENABLE=0、FAULTED=1、正在中止、命令 FIFO 满或字段非法时拒绝 PUSH，不改变队列；记录对应 sticky 错误。CMD_PUSH=0 无操作。保留位忽略。

#### Acceptance Criteria

- 对照原合同 CMD-005 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.FUNC.SPI_MASTER.CMD.006

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CMD.006
category: FUNC
feature: CMD-006
priority: P0
status: active
source_ref:
- spi_master_contract.md#CMD-006
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

CS 的模式、分频、时序、位序、dummy pattern 在 ENABLE=1 时只读；软件必须等 BUSY=0 后清 ENABLE 再修改。这消除了“已排队命令引用配置被悄悄修改”的歧义。命令 shadow 和 FIFO 数据口在使能期间可访问。

#### Acceptance Criteria

- 对照原合同 CMD-006 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.FUNC.SPI_MASTER.CMD.007

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CMD.007
category: FUNC
feature: CMD-007
priority: P0
status: active
source_ref:
- spi_master_contract.md#CMD-007
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

数据命令第一次启动前检查 TX/RX 所需资源。资源不足时不提前断言 CS；BUSY=1，进入 WAIT_TX/WAIT_RX。DUMMY 无 FIFO 依赖。RELEASE 在无活动 CS 时合法，为无引脚变化的完成命令。

#### Acceptance Criteria

- 对照原合同 CMD-007 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.FUNC.SPI_MASTER.CMD.008

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CMD.008
category: FUNC
feature: CMD-008
priority: P0
status: active
source_ref:
- spi_master_contract.md#CMD-008
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

每个 frame 开始前取得完整 TX entry（如需要）并预留完整 RX entry 空间（如需要）；一旦 frame 开始，不得因 FIFO 流控在 frame 中间停 SCLK。

#### Acceptance Criteria

- 对照原合同 CMD-008 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.FUNC.SPI_MASTER.CMD.009

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CMD.009
category: FUNC
feature: CMD-009
priority: P0
status: active
source_ref:
- spi_master_contract.md#CMD-009
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

TX/RX 两种资源同时需要时须原子取得：RX 没有空间不得预先不可逆弹出 TX。移位寄存器和预留槽应单独计入设计状态，防止“FIFO 空”被误认为最后一帧已发送。

#### Acceptance Criteria

- 对照原合同 CMD-009 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。

### LRS.FUNC.SPI_MASTER.CMD.010

<!-- LRS_META
id: LRS.FUNC.SPI_MASTER.CMD.010
category: FUNC
feature: CMD-010
priority: P0
status: active
source_ref:
- spi_master_contract.md#CMD-010
applicability:
  expr: 'true'
verification_method:
- simulation
- review
END_LRS_META -->

#### Requirement

最后一帧/最后 Dummy 周期必须完成末尾 trailing edge。KEEP_CS=0 时，进一步满足 CS_HOLD 并释放 CS 后才产生 SEG_DONE；KEEP_CS=1 时，在末尾 trailing edge 且 RX 数据提交完成后产生 SEG_DONE，进入保持或执行下一段。

#### Acceptance Criteria

- 对照原合同 CMD-010 的全部条件，检查可观察结果及异常路径；不得以成功通路替代边界检查。
