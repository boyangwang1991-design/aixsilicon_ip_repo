# 后续流程持续授权

用户在 G2 交接后明确指示：`finish the rest, you don't have to ask for permission`。
前一条授权为 `approve, complete the rest`。据此继续剩余完整流程，不再逐阶段请求许可。
授权对象为本工作区 watchdog full-flow 交付和必要修正，包含 LLD、寄存器、VPLAN、RTL、验证与交付检查。
本记录不冒充独立架构/RTL/DV 人工评审。技术门禁仍由具体检查和实测证据决定；不以授权替代测试，不将未运行标为通过。
冻结的 LLD 作者检查输入保存在 g2_approved_snapshot.json。后续语义修正必须同步文档、模型并重新验证。
