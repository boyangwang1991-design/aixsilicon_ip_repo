# LLD 恢复核验与委托评审

依据 [持续授权](continuation_authorization.md)，本次由 AI 核验当前 LLD 输入，
属于用户委托检查；不冒称独立 RTL/DV 评审或 EDA 通过。

本轮移除六处已废弃且重复的 illegal_state_policy 字段，保留原有
illegal_state_handling 行为描述；没有改变状态转移、错误优先级或外部行为。
每次修改后调用 owning extractor，最终全目录抽取及 check_lld.py 通过。
检查覆盖 139 条需求、六个模块、22 接口、97 字段、六个 FSM、六条 CDC 路径、
16 参数和 167 个设计对象；RDL 字段引用及正文/模型一致性已核对。

继续保留候选年龄到期优先、服务完整资格复算、独立 final_hold、整组配置拒绝、
无撕裂邮箱与最多一次执行、恢复四相握手及最终请求保持的既定约束。
六处 schema 清理不证明 RTL 已正确实现这些语义。

本记录只确认当前字段行为和微架构输入，可继续生成/核验 CSR 和 VPLAN。
整体 G2 仍需当前 CSR 生成与编译证据；G3–G5、网表冗余与物理共因分析另行核验。
本次输入指纹包含恢复后的上游审批；时间表示本次检查时间，不是历史用户签名时间。
