# GPIO 覆盖率验收

功能覆盖目标100%；代码line/branch/condition/toggle目标分别100%；已启用SVA的成功触发覆盖目标100%，失败计数为0。按配置合并并保留原始VCS/URG数据库。排除项仅允许静态裁剪不可达、参数非法保护及有独立证明的不可达状态，逐项记录原因、配置、owner和审批来源。未达到目标保持G4失败，不以修改目标结案。

[域1](coverage_plan_1.md)、[域2](coverage_plan_2.md)。必须覆盖合同§19全部12组边界交叉。PPA目标需要集成工艺/时钟约束输入，缺少时保留未签核。
