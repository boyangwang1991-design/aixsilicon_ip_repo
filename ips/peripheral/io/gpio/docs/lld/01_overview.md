# GPIO 微设计概述

沿用HLD的12个模块。Bank宽度32，N_BANK=(N_GPIO+31)/32；寄存器地址保持最大地图，实例范围外由几何检查拒绝。
生成CSR覆盖最大128路地图，固定结构的字段由外部业务owner驱动；未用实例位始终屏蔽，综合可裁剪。
全局/Bank/Pin/AON寄存器的读数据由所属状态组合产生，通过PeakRDL外部寄存器读接口返回。
RDL确定性派生访问描述符仅用于权限、能力、几何及业务路由，不手写第二套地址常量。
普通状态使用main复位，锁/访问策略/内部parity故障及主域mailbox传输身份使用POR复位，AON状态仅AON冷复位。
