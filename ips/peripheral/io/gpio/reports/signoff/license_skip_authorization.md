# GPIO CDC/RDC 许可证例外授权

用户原话（2026-09-14，本次会话）：

> 由于缺失CDC ADV的LICENCE，约束SKILL，可以先跳过这个过程，如实记录即可，不要阻塞后续流程

范围：GPIO 的 CDC/RDC 高级许可证缺失过程；RDC 原始报告同时缺少 cdc_adv_checker 和 rdc_adv_checker。允许继续后续开发，签核状态为 skipped，未运行的规则仍未验证。此授权不表示技术通过，不豁免其他检查或实际违例。许可证可用后对届时设计重跑完整目标。
