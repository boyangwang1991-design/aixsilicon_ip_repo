# G0 Sign 时序策略确认记录

先前 G0 及吞吐预算授权见 [原确认](g0_authorization.md)和[吞吐变更](g0_throughput_authorization.md)。

用户询问：“你推荐哪一种解决方案”。助手明确推荐：每次尝试固定调度、总延迟可变，
不暴露拒绝原因；保留安全重试上限，超限统一错误并清零；P50/P95/P99 性能验收，
另验证时延分布不会泄露秘密。用户随后指示：“continue”。

本记录按连续对话语义将继续指令解释为采用该推荐方案，未将其扩大为 G1/G2 冻结。
本轮仅修改 LRS.SEC.PQC.CT.001 与 DSA_SIGN.001 的时序边界，原需求 ID 保持稳定。
采样等待也计入固定预算，原始执行证据未因此改写，未声明 RTL 或侧信道验证通过。

当前 G0 输入指纹：`24a5a85847972cca78e4f5b39c32287aa5a113538ff05399f8d6c581faa62018`。
