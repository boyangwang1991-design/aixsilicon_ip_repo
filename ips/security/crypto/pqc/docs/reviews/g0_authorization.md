# G0 用户确认记录

2026-09-16，助手请求：“请确认是否批准当前 LRS 的 G0 冻结，以继续下游设计与修复。”

用户原文：“确认，请继续”。

确认范围是当时展示的 84 条 LRS，不延伸为未完成 RTL、G1/G2 或验证签核。
被批准输入指纹（套件 design_provenance --stage lrs）：
`ec11269e0942984f370f6f461663d1df93209874cc0379cdea61dacbad9da2f1`。

用户此前确定长期密钥由外部 Key Manager 所有、内部仅保留工作态安全 Key RAM，
私钥专用导入且普通 AXI DMA 不可读回；PPA 本轮仅评估代码结构。
执行者仅记录真实确认，不冒称独立评审机构或替代工具检查。
