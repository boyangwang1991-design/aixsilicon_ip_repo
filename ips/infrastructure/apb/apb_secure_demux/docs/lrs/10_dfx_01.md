# 受控诊断（1）

来源：输入契约的 DFX 需求族。以下条目与同 source_ref 的其他条目共同保持原文语义。

### LRS.DFX.APB_SECURE_DEMUX.DFX.00101

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.00101
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-001
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

DFX 控制只在 DFX_EN=1、管理授权和 dfx_authorized_i=1 时允许。

#### Acceptance Criteria

- 应满足：DFX 控制只在 DFX_EN=1、管理授权和 dfx_authorized_i=1 时允许。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.00102

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.00102
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-001
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

DFX 硬件授权不能由本 IP 的 CSR 自行产生。

#### Acceptance Criteria

- 应满足：DFX 硬件授权不能由本 IP 的 CSR 自行产生。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.00201

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.00201
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-002
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

授权变低时，下一时钟边沿解除所有未触发注入武装；观测输出受授权门控。

#### Acceptance Criteria

- 应满足：授权变低时，下一时钟边沿解除所有未触发注入武装；观测输出受授权门控。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.00202

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.00202
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-002
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

已经完成的事件和计数不自动清除。

#### Acceptance Criteria

- 应满足：已经完成的事件和计数不自动清除。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.003

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.003
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-003
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

SUCCESS_COUNT 只统计下游 PREADY=1 且 PSLVERR=0 的完成事务；DENY_COUNT 只统计唯一命中该端口的拒绝；SLVERR_COUNT 统计下游错误。

#### Acceptance Criteria

- 应满足：SUCCESS_COUNT 只统计下游 PREADY=1 且 PSLVERR=0 的完成事务；DENY_COUNT 只统计唯一命中该端口的拒绝；SLVERR_COUNT 统计下游错误。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.004

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.004
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-004
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

所有 32 bit 计数饱和不回绕；同周期清除和递增时先清零再累加本周期事件。

#### Acceptance Criteria

- 应满足：所有 32 bit 计数饱和不回绕；同周期清除和递增时先清零再累加本周期事件。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.00501

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.00501
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-005
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

WAIT_TOTAL 只计算下游 PSEL && PENABLE && !PREADY 周期，不包括寄存模式额外 SETUP 周期。

#### Acceptance Criteria

- 应满足：WAIT_TOTAL 只计算下游 PSEL && PENABLE && !PREADY 周期，不包括寄存模式额外 SETUP 周期。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.00502

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.00502
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-005
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

WAIT_MAX 在完成时更新；未完成事务通过硬件阈值输出观测。

#### Acceptance Criteria

- 应满足：WAIT_MAX 在完成时更新；未完成事务通过硬件阈值输出观测。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.006

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.006
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-006
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

WAIT_THRESHOLD=0 禁用告警；T>0 时，在单笔第 T 个等待周期边沿触发一次 WAIT_EXCEEDED 并置 WAIT_HIT；同笔不重复触发，新事务重新计数。

#### Acceptance Criteria

- 应满足：WAIT_THRESHOLD=0 禁用告警；T>0 时，在单笔第 T 个等待周期边沿触发一次 WAIT_EXCEEDED 并置 WAIT_HIT；同笔不重复触发，新事务重新计数。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.00701

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.00701
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-007
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

busy_o 表示有外设事务正在 SETUP/ACCESS；active_port_valid_o 随有效目标，active_port_o 无效时为 0。

#### Acceptance Criteria

- 应满足：busy_o 表示有外设事务正在 SETUP/ACCESS；active_port_valid_o 随有效目标，active_port_o 无效时为 0。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

