# 受控诊断（2）

来源：输入契约的 DFX 需求族。以下条目与同 source_ref 的其他条目共同保持原文语义。

### LRS.DFX.APB_SECURE_DEMUX.DFX.00702

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.00702
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-007
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

仅诊断信号，不作为授权凭据。

#### Acceptance Criteria

- 应满足：仅诊断信号，不作为授权凭据。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.008

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.008
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-008
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

wait_threshold_o 表示授权条件下的粘滞 WAIT_HIT，清除由 DFX_CLEAR[0]；不会终止下游事务。

#### Acceptance Criteria

- 应满足：wait_threshold_o 表示授权条件下的粘滞 WAIT_HIT，清除由 DFX_CLEAR[0]；不会终止下游事务。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.00901

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.00901
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-009
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

INJECT_TARGET 写入时检查端口和主体范围；武装时锁存目标。

#### Acceptance Criteria

- 应满足：INJECT_TARGET 写入时检查端口和主体范围；武装时锁存目标。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.00902

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.00902
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-009
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

武装期间改目标或再次武装返回 CMD_INVALID，须先解除。

#### Acceptance Criteria

- 应满足：武装期间改目标或再次武装返回 CMD_INVALID，须先解除。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.01001

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.01001
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-010
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

强制拒绝为一次性；匹配 MASTERID/端口、原本合法的下一笔事务在 SETUP 被拒绝，产生 DFX_FORCED_DENY，完成时计拒绝、bit0 与 bit8。

#### Acceptance Criteria

- 应满足：强制拒绝为一次性；匹配 MASTERID/端口、原本合法的下一笔事务在 SETUP 被拒绝，产生 DFX_FORCED_DENY，完成时计拒绝、bit0 与 bit8。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.01002

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.01002
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-010
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

自然非法事务不消耗武装。

#### Acceptance Criteria

- 应满足：自然非法事务不消耗武装。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.01101

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.01101
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-011
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

完整性故障注入为一次性；匹配的下一笔原本合法事务在 SETUP 阻断，在 SETUP 结束边沿置全局 FATAL，产生带 TEST 的 INTEGRITY_DETECTED；该笔完成再产生 INTEGRITY_BLOCK，标记 TEST。

#### Acceptance Criteria

- 应满足：完整性故障注入为一次性；匹配的下一笔原本合法事务在 SETUP 阻断，在 SETUP 结束边沿置全局 FATAL，产生带 TEST 的 INTEGRITY_DETECTED；该笔完成再产生 INTEGRITY_BLOCK，标记 TEST。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.01102

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.01102
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-011
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

后续访问按普通 FATAL 拒绝。

#### Acceptance Criteria

- 应满足：后续访问按普通 FATAL 拒绝。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.01103

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.01103
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-011
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

该模式需 POLICY_PARITY_EN=1，否则命令错误。

#### Acceptance Criteria

- 应满足：该模式需 POLICY_PARITY_EN=1，否则命令错误。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

### LRS.DFX.APB_SECURE_DEMUX.DFX.012

<!-- LRS_META
id: LRS.DFX.APB_SECURE_DEMUX.DFX.012
category: DFX
feature: dfx
priority: P0
status: active
source_ref:
- REQ-DFX-012
applicability:
  expr: 'true'
verification_method:
- simulation
- formal
END_LRS_META -->

#### Requirement

DFX 完整性注入测试“检测结果至阻断/告警”路径，不翻转实际权限存储位，不能代替验证环境的真实 parity 位故障注入测试。

#### Acceptance Criteria

- 应满足：DFX 完整性注入测试“检测结果至阻断/告警”路径，不翻转实际权限存储位，不能代替验证环境的真实 parity 位故障注入测试。
- 覆盖功能裁剪、硬件授权高低及撤销边沿；对比武装、计数、输出和 TEST 标记。

